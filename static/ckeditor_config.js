CKEDITOR.editorConfig = function (config) {
    // Reduce the number of items in the toolbar
    config.removeButtons = 'Image,Flash,Table,HorizontalRule,Smiley,SpecialChar,PageBreak,Iframe';

    // Adjust the width of CKEditor
    config.width = '800px';
};
